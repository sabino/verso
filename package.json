{
  "name": "verso",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A procedural multiplayer browser RPG of unique lives, generated civilizations, crafting, and mind travel.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build && node scripts/build-offline.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "check": "npm test && npm run build",
    "format": "prettier --write .github migration src tests scripts docs *.ts *.html *.json README.md",
    "format:check": "prettier --check .github migration src tests scripts docs *.ts *.html *.json README.md",
    "server": "node --experimental-strip-types server/index.mjs"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "vite": "^7.1.5"
  },
  "dependencies": {
    "peerjs": "^1.5.5",
    "qrcode-generator": "^2.0.4",
    "ws": "^8.21.3"
  }
}
